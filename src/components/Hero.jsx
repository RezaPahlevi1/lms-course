// --- Hero with search and filters ---
export function Hero({ children }) {
  return (
    <section className="relative bg-linear-to-r from-black to-[#293352]">
      <div className="px-6 py-12">{children}</div>
    </section>
  );
}
